<h2><%= t :cmm_chat_application %></h2>
  <nav class="navbar navbar-expand-lg bg-light navbar-light">
    <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="#"><%= current_user.name %></a>
        </li>
        <li class="nav-item">
          <div class="dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <%= @workspace.name %>
              </a>              
              <div class="dropdown-menu" >
                <%= link_to "#{t('create')}", new_workspace_path,class: "dropdown-item" %>
                <%= link_to "#{t('edit')}", edit_workspace_path,class: "dropdown-item" %>
                <%= link_to "#{t('delete')}", workspace_path, method: :delete, data: { confirm: 'Are you sure?' },class: "dropdown-item" %></td>
                <%= link_to "#{t('invite')}",workspace_userworkspace_index_path(@workspace.id),class: "dropdown-item" %>
                 <%= link_to "#{t('changews')}", '/search' ,class: "dropdown-item" %>
              </div> 
          </div> 
        </li>
        <li class="nav-item">   
          <div class="dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown1" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <%= t('group') %> 
              </a>
              <div class="dropdown-menu">                  
                 <%= link_to "#{t('create')}", new_group_path,class: "dropdown-item" %>
              </div>
          </div>    
        </li> 
        <li class="nav-item">
        <div class="dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown2" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <%= t('friend') %></a>
          <div class="dropdown-menu">                  
           <% if !@userworkspace.nil? %>
            <% @userworkspace.each do |wkuser| %>
                <% if wkuser.level == "owner" %>
                   <span><%= wkuser.user.name %>-<%= t('you') %></span><br>     
                <% end %>
                <% if wkuser.level == "member" %>
                   <span><%= wkuser.user.name %></span><br>
                <% end %>
             <% end %>
            <% end %>
          </div>
        </div>
        </li>       

        <li class="nav-item">
          <a class="nav-link" href="#"><%= t('starred') %></a>
        </li>    

        <li class="nav-item">
        <div class="dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown5" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <%= t('about') %></a>
        <div class="containersize">
          <div class="dropdown-menu">                
           <p><h4>TEAM 5 </h4></p>
           <p>Shoon Lae Khaing</p>
           <p>Yin Nyein Aye</p>
           <p>Zin Zin Myo Lwin</p>
           <p>May Thu Myat</p>
          </div>
         </div>
         </div>
        </li>
        <li class="nav-item">
          <%= link_to("#{t('logout')}", logout_path, method: :delete) %> 
        </li> 
    </ul>
  </nav>
  <div class="wrapper"> 
    <nav id="sidebar"> 
      <h4><%= t('group') %></h4>  
        <ul class="sidebarul">          
          <% @groups.each do |group| %>
            <li>
              <div class="dropdown">
                  <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown3" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                   <%=  group.name %></a>
                <div class="dropdown-menu">
                  <ul>
                    <%= link_to fa_icon("pencil"),edit_group_path(group.id),class: "dropdown-item" %><span><%= t('edit') %></span>
                    <%= link_to fa_icon("trash-o"), group_path(group.id), method: :delete, data: { confirm: 'Are you sure?' },class: "dropdown-item" %><span><%= t('delete') %></span>
                    <%= link_to fa_icon("user-plus"),group_groupuser_index_path(group.id),class: "dropdown-item" %><span><%= t('invite') %></span>
                    <%= link_to fa_icon("envelope"),group_path(group),class: "dropdown-item" %><span><%= t('message') %></span>         
                  </ul>
                </div>
              </div>
            </li>
          <% end %>  
        </ul>   
    </nav>
  </div>
  



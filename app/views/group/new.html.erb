<h2><%= t('create_group') %></h2>
<div class="newgroup">
  <%= form_for(@group, url: {action:"create"}) do |form| %>
    <div class="field">
      <%= form.label :"#{t('group_name')}", class: "col-md-4 control-label" %>
        <div class="col-lg-5">
           <%= form.text_field :name, name:"name", class: "form-control" %>
        </div>
    </div>
    
    <div class="form-group">
      <%= form.label :"#{t('purpose')}", class: "col-md-4 control-label" %>
        <div class="col-lg-5">
          <%= form.text_field :purpose, name:"purpose",class: "form-control" %>
        </div>
    </div>

    <div>
        <%= form.hidden_field :workspace_id, value: @workspace.id ,name:"workspace_id" %>
    </div>

    <div class="col-lg-5">
      <%= radio_button_tag(:ass_typ, "true") %><%= t :public %>
      <%= radio_button_tag(:ass_typ, "false") %><%= t :private %>
    </div> 

    <% if @group.errors.any? %>
          <div id="errorExplanation">
            <ul>
              <% @group.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
              <% end %>
            </ul>
          </div>
        <% end %>   
    
    <p id="demo"></p>
 
    <div class="col-lg-5">
      <%= form.submit "#{t('create')}", class: "btn btn-primary" %>
      <%= link_to "#{t('cancel')}",   @workspace , class:"btn btn-primary margin-left" %> 
    </div>
  <% end %>
</div>
<script>  
function add_ass_typ(a){      
    }
</script>
  

 

